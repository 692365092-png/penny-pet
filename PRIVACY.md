# Privacy / 隐私说明

Penny pet 的便利贴、提醒、动画和大多数功能都在本地运行。只有用户主动开启的“本地天气”会访问 Open-Meteo。

- 便利贴、待办、日程、提醒和设置保存在 `%LocalAppData%\PennyPet`。
- 程序不包含广告、用户账户或遥测，也不会上传便利贴、待办、日程、提醒、键盘内容、姓名、星座或传统日历结果。
- “按键显示”默认关闭；关闭时不会启动全局键盘监听。
- 用户主动开启“按键显示”后，程序只把当前按键名称短暂显示在桌宠旁边，不保存到磁盘，也不发送到网络。
- 对 Windows 能识别的密码框和敏感输入位置，程序会尽力隐藏按键内容；第三方、自绘、跨权限或远程窗口不保证都能被识别，处理高敏感信息时应关闭按键显示。
- “联系作者”只在用户主动点击时使用默认浏览器打开公开的小红书主页。

## 可选的本地天气

- 本地天气默认关闭。Penny 不使用 Windows 定位、GPS、IP、Wi-Fi 或浏览器定位；城市必须由用户手动搜索并从结果中确认。
- 搜索城市时，输入的城市名称会直接发送给 Open-Meteo Geocoding API。选中后只在本机长期保存当前城市名称、行政区、国家、经纬度和时区；更换城市会覆盖这些偏好。
- 开启天气后，Penny 只在每天第一次符合条件的“每日内容”戳击中，把已保存城市的经纬度和时区直接发送给 Open-Meteo Forecast API。启动时不请求天气，也不轮询。
- 预报只保存在当前进程内，按地点和当地日期覆盖，最多保留 3 个日期键；退出程序后消失，不写入磁盘，也不积累天气历史。失败冷却状态同样只在进程内覆盖保存。
- Open-Meteo 的免费服务可能为故障排查记录请求 IP 和请求坐标，并说明这些日志会在 90 天后删除。其处理规则见 [Open-Meteo Terms and Privacy](https://open-meteo.com/en/terms)。
- 关闭天气后不再发起预报请求；已选择城市作为用户偏好保留，方便以后重新开启。

如发现隐私或安全问题，请通过仓库的 Security Advisory 私下报告，不要在公开 Issue 中粘贴个人数据。

## 在线功能的开发原则

除上述可选天气外，当前没有其他在线内容服务。后续开发仍必须遵守：

- 在线增强功能默认不得上传便利贴、待办、日程、提醒正文、键盘内容或无关设备信息。
- 每个功能只发送实现该功能真正需要的最少字段（data minimization）。
- 网络不可用时，桌宠主体和全部本地功能必须继续工作。
- 若未来某项功能确实需要用户数据，必须先明确说明用途、字段、保存期限和接收方，并提供用户可理解的选择；不能通过普通版本更新静默扩大收集范围。
- 远程内容缓存必须和用户核心数据分开，且不能成为保存私人内容的新位置。
- 诊断日志不得记录请求/响应正文、认证 token 或本地用户内容。
