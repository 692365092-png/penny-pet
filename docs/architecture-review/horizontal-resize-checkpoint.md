# 横向 resize 工作检查点（未验收）

工作区清理后，从已提交的 00b7067 恢复了进行中的改造：统一 DockResizeSession、横向开始/完成事件、原生 follower 批次、各窗口 DPI 下的尺寸偏好和 final 后的相关用户操作；Dock 拖拽保存进入已有异步 writer。后续复查确认实时快照原本已是内容快照，不构成新的性能收益。

这是保全代码的 WIP，不是已完成的验收。源码边界断言尚需迁移，横向行为回归尚需补齐，构建与 Windows 验收尚未完成。已有 410 项通过记录只适用于 00b7067，不适用于本检查点。

后续状态：本检查点的实现和回归工作已由 [横向 resize 审查](2026-09-13-horizontal-resize-review.md) 接续完成；当前证据为 440 次辅助调用通过和两项编译通过。Windows 原生验收仍未完成。上文保留为 c0ad4c5 当时的记录。
