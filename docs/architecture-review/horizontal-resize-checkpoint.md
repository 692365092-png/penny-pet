# 横向 resize 工作检查点（未验收）

工作区清理后，从已提交的 00b7067 恢复了进行中的改造：统一 DockResizeSession、横向开始/完成事件、原生 follower 批次、各窗口 DPI 下的尺寸偏好和 final 后的相关用户操作。实时事件改用内容快照；Dock 拖拽保存进入已有异步 writer。

这是保全代码的 WIP，不是已完成的验收。源码边界断言尚需迁移，横向行为回归尚需补齐，构建与 Windows 验收尚未完成。已有 410 项通过记录只适用于 00b7067，不适用于本检查点。
