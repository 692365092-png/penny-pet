# 整组恢复工作检查点

在 410dc4f 基础上实施：普通 preferred 恢复合为一次 STA 命令；DockRestoreOperation/Operations 持有恢复意图、拓扑、成员身份及取消；移除 PetForm 的逐窗准备计数和重复终止分支。失败只清理新建窗口，旧可见窗口使用已有事务回滚。

这是回归补齐前的 WIP 保全检查点，不是验收完成。440 项通过记录属于上一轮 410dc4f；本轮仍需迁移源码边界检查、补行为测试、验证最新源码并记录结果。旧格式物理 fallback 和 Windows 原生验收仍开放。
