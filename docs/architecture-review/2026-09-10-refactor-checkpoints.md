# 重构检查点

这是连续工作记录，不是发布验收。最新结论见 [整合审查](2026-09-12-integration-review.md)。

| 检查点 | 内容 | 状态 |
| --- | --- | --- |
| `aa31d7d` | 在 Pet 侧选择 standalone 放置计划再跨 STA 传递 | 已提交 |
| `43e088c` | 正常几何事实、Dock 消费者、退出最终快照 | 已提交；当时 11 个旧源码断言待迁移 |
| `dff526b` | 保留用户上传的 ab522d9 保存补丁 | 已提交，作为可追溯的审查输入 |
| `b0cb8b7` | 主文件单写入者、独立紧急导出、唯一临时文件、迁移几何断言 | 编译与 373 次直接测试调用通过 |

后续补充了普通/自动保存对隐藏 Dock 槽位和独立高度的实际文件回归。最终统计和重现命令见 [验证证据](integration-evidence/README.md)。

补丁原先把导出排进主保存队列。审查后调整为独立文件导出，且导出不能清除主文件 dirty 或覆盖主文件及其滚动备份。主文件仍只有一个 writer，设置属于独立文件。该修正也移除了更底层的进程全局 I/O 锁。

PC-6 Dock 状态所有权、PC-6/PC-8 双重关系、PC-7 PetForm 责任拆分仍未完成。Windows HWND、IME、焦点、混合 DPI 和整包验收仍开放。
