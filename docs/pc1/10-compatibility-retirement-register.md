# Compatibility retirement register

Baseline `ab10b45`. Legacy is not a deletion verdict. Reader/writer method locations are indexed in 04 and geometry-read-write.csv. Earliest phase means **review eligibility**, not permission granted now.

| Concept / class | Why / current readers and writers | User-data + runtime need | Runtime retirement / persisted retirement / earliest / blocker |
|---|---|---|---|
| StickyNoteData X/Y/W/H — RECOVERY ONLY target, live bridge currently | historical physical rect; codec, fallback show/restore, Dock preview/layout, SideTabs overlap read; actual facts/target adapters and codec write | v1+ recovery and old backups; still live consumers | runtime normal reads after PC-3 migration; persisted removal not assumed; PC-8 only with proven bounded recovery alternative |
| DisplayId — RUNTIME TEMP + READ-ONLY COMPAT | v10 runtime GDI identity; ResolvePlacementPlan, MigrateV10Preferred, CommitExpandedPreferred; facts adapter/codec write | old saved monitor intent and current mirror logic | eliminate normal reader authority PC-3, retain decoder; cannot remove serialized field before PC-8/schema decision |
| LocalLogicalX/Y/W/H — RUNTIME TEMP + READ-ONLY COMPAT | v10 local rect; **PlanDockPlan uses local height and requires positive local dimensions**, restore/show/migration/tile read; facts/codec write | currently necessary normal Dock input, not migration-only | PC-3 must replace planner + show/restore readers first; PC-8 serializer retirement only after fixtures |
| Preferred* — KEEP (durable contract) | v11 user intent; show/restore/rehome/return read; explicit allowed user commit/missing migration/codec write | preserves temporary-rehome distinction | no deletion goal; possible value type only after authority tests |
| v1–v11 readers — READ-ONLY COMPAT | StickyNoteCodec.ParseLine + backup validator + fixtures | user-created long-lived content/upgrades | retain historical readers, do not delete by age; newer writer may change only in approved PC-8 |
| v10 serialized mirror — RUNTIME TEMP | current v11 SerializeLine still emits v10 positions | existing wire contract and old reader safety | no field reorder/removal PC-1; v12 not designed until PC-3 complete |
| v11 writer — KEEP until explicit schema migration | all three field families plus content/relation | current on-disk truth | PC-8 only; retain future-schema block/backup protection |
| MigrateV10Preferred — MIGRATION ONLY | missing preferred upgraded with resolved live topology; AdoptPreferredIfEmpty + Dock restore callers | preserves original v10 local intent | boundary confinement PC-3/8, not removal while v10 fixtures supported |
| physical fallback restore — RECOVERY ONLY | session no resolvable preferred/v10 -> largest intersection or primary work area; Pet fallback policy | absent monitor/old records still need safe display | retain as explicit bounded recovery; no evidence it can be deleted |
| legacy DockParent chain fallback — READ-ONLY COMPAT + RUNTIME TEMP | BuildDockChainOrder/Core ordered-group rules, legacy restore key | group recovery with missing durable group metadata | normal runtime chain policy PC-6; historical decode fallback persists PC-8 |
| source-string architecture guards — DELETE AFTER equivalent PC-2/10 tests | read production source/helper names; only tests write assertions | no user-data role; protects intended boundaries imperfectly | isolate PC-2, replace later; no deletion without invariant/behavior coverage |
| PetForm partial files — RUNTIME TEMP organization | one private field namespace, constructor/Form shutdown shared | no storage format role; current orchestration must remain functional | PC-4/6 before PC-7 real ownership; filename deletion is not architecture success |
| hosted sequence compatibility methods — RUNTIME TEMP API, essential safety | CanApplySequence/RecordSequence used by events and single/batch results | stops content rollback | can consolidate checks PC-5/6 but preserve per-note lease/sequence behavior |
| restore SynchronizeSessionLease — KEEP until lifecycle redesign proven | EnsureSession ack resets Pet accepted seq, preserves IME/focus/delete | recreated window otherwise rejects all new events | PC-5 requires recreated-session+effective-registry characterization; no arbitrary max() replacement |
| pending restore gate — KEEP transaction invariant | TryRestoreHostedDockComponent Add; success/fail finally release | duplicate reopen correctness | PC-6/7 may move owner, not remove gate |
| topology epoch-zero plans — RUNTIME TEMP protocol distinction | Host ApplyDockPlan permits epoch0 for topology work | topology not part of current gesture | PC-5/6 named intent/token proof before narrowing; can't require positive epoch universally |
| DRT display diagnostics — RUNTIME TEMP / UNKNOWN removal list | DisplayDiagnostics -> bounded ApplicationDiagnostics log; mismatch/rejection/restore events | no durable user data; useful for mixed-DPI failure diagnosis | PC-10 retain actionable events and retire experimental noise based on measurement; no log schema expansion now |
| future-schema read-only protection — KEEP FOREVER as invariant | repository primary preflight/BlockFutureSchema, write guard, backup protection | old reader must not fall back to old backup and overwrite newer data | no retirement; schemas can evolve, invariant cannot |

No register row is a PC-1 deletion authorization. H-GEO-1 supports a two-layer normal runtime only **after** its remaining live readers migrate. The physical recovery value remains a legitimate exception.
