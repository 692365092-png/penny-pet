namespace PennyPet
{
    // Immutable identity token stamped at the native input boundary. It is
    // intentionally not a gesture owner; live Dock ownership is Sticky-local.
    internal sealed class DockInput { }
}
